"""
Training entry point. Empty stub -- fill in once env.py actually works
end-to-end against a running Dolphin instance.

Rough shape this will probably take:

    from env import TanksEnv
    # from stable_baselines3 import PPO  # once stable-baselines3 is added

    def main():
        env = TanksEnv()
        # model = PPO("MlpPolicy", env, verbose=1)
        # model.learn(total_timesteps=...)
        # model.save("tanks_ppo")

    if __name__ == "__main__":
        main()
"""
